import React from 'react';
import { Button } from "@material-ui/core"

function Header(props) {
return (
<div>
<h4>Header </h4>
<Button variant="outlined">Start Race</Button>
</div>
);
}

export default Header;
